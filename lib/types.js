// Type definitions are documented here for reference.
// Since we're using JS, these serve as documentation only.
//
// MenuItem: { id, name, description, price, category, image }
// Testimonial: { id, name, text, rating }
// Location: { id, name, address, hours, lat, lng }
// User: { username, role }
// BrainDump: { id, content, created_at, user_id }
